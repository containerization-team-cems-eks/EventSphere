replicaCount: 2

image:
  repository: ""  
  tag: "latest"    
  pullPolicy: IfNotPresent

namespace: dev

serviceAccount:
  name: event-service-sa

configMap:
  name: event-service-config

secrets:
  mongodb:
    name: mongodb-secret
    key: connection-string
